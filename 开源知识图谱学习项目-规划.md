# 「AI 教你学 AI」开源知识图谱项目 — 规划

> **撰写日期**：2026-05-24  
> **状态**：规划稿（待你确认后拆仓库 / 建目录）  
> **关联**：[`30天-agent学习路线/README.md`](./30天-agent学习路线/README.md)、仓库根目录 [`AI知识树大纲/`](../AI知识树大纲/)、Obsidian 配置 [`.obsidian/`](../.obsidian/)

---

## 1. 对现有 30 天路线的诊断

### 1.1 做得好的地方（建议保留）

| 点 | 说明 |
|----|------|
| **学习顺序** | Prompt → Context → Harness → Model，与 2025–2026 业界分层一致，适合「先 Agent 应用、后模型」 |
| **可验证产出** | 每日 3 条事实 + 层级标注，避免「看过 = 学会」 |
| **外部资源锚定** | 以 Anthropic / Karpathy / HF 等一手材料为主，适合写进知识节点的 `sources` |
| **节奏** | 2–3h/天 + 10 分钟资讯，对个人可持续 |

### 1.2 与「知识图谱 + 开源项目」目标的缺口

| 缺口 | 表现 | 影响 |
|------|------|------|
| **只有路线，没有原子知识** | `README` 是日历表，知识点未拆成可链接的独立条目 | Obsidian 图谱无法生长 |
| **笔记与知识未分离** | `notes/D{n}.md` 是个人学习日志，不宜直接当百科 | 开源后他人无法复用「概念页」 |
| **分类维度单一** | 仅用四层栈（Prompt/Context/Harness/Model） | 缺「知识类型」「难度」「先修」等多轴 |
| **无统一文档壳** | 没有「30s → 深入 → 示例」模板 | 贡献者写法不一致，AI 也难稳定生成 |
| **与 `AI知识树大纲` 未打通** | 公司向大纲在另一目录，粒度与 Agent 路线不同 | 重复建设、链接断裂 |
| **「AI 教你学」未产品化** | 未定义 Agent 如何读图谱、推下一节、出题 | 想法停在「用 Cursor 自己看」 |

### 1.3 阶段顺序要不要改？

**结论：主线不用推倒重来，但要拆成两条轨。**

| 轨道 | 内容 | 关系 |
|------|------|------|
| **轨 A：个人执行** | 继续走 30 天日历（可按自己进度） | 学习日志写 `notes/`，不进入开源主库 |
| **轨 B：开源知识库** | 按模板沉淀「概念页」，日历只**引用**概念 ID | 你学一章 → 合并一篇概念 PR |

**可选微调（非必须）：**

- 若你近期就要做 Obsidian 图谱：**W1–W3 每学完 1 天，沉淀 1 个概念页**（哪怕 stub），比 W4 再集中写 Model 更易坚持。
- D10 RAG 可提前标为「与 Context 强相关」，在分类上归入 `type: context` + `tag: rag`，避免 RAG 变成孤岛目录。

---

## 2. 项目定位与差异化（「自己的特点」）

### 2.1 一句话定位

**面向自学者的、Agent-First 的 AI 知识图谱：每个概念都能被人和 AI 在 30 秒内定位，再按需深入，并通过 Obsidian 图谱与可执行学习路线闭环。**

### 2.2 与常见资料的区别

| 常见形态 | 本项目 |
|----------|--------|
| 线性教程 / 视频课单 | **图谱 + 多路线**（同一概念可从 Agent 轨或 Model 轨进入） |
| 维基式长文 | **分层阅读**（30s / 深入 / 示例分块，链接跳概念） |
| 纯笔记仓库 | **机器可读 frontmatter**（供 Agent 选题、先修检查、测验） |
| 公司内训大纲 | **开源 + 可 fork**；公司大纲通过 `maps/enterprise-bridge.md` 映射，不混正文 |

### 2.3 「AI 教你学 AI」具体指什么

不是替代老师，而是仓库内提供 **可复制的 Agent 契约**：

1. **读图谱**：根据 `progress.yaml` + 概念 `prerequisites` 推荐下一篇  
2. **讲概念**：只展开用户选的层级（先要 30s 还是要示例）  
3. **出题验收**：用节点内 `checks` 生成选择题 / 情景题（对齐你 `AI知识树大纲` 里的验证思路）  
4. **写回进度**：更新学习者的 fork 或私有 `progress.yaml`（不进主库 PR）

实现可分期：第一期人工 + Cursor rules；第二期脚本 + 可选小 CLI。

---

## 3. 第一层分类：知识「类型」轴

在 Obsidian 与 Git 中，**每个概念页必须有 `type`**（第一层分类）。建议 **MECE 且与 Agent 栈可交叉**：

| `type` | 中文 | 典型内容 | 对应 30 天周次 |
|--------|------|----------|----------------|
| `concept` | 概念 | 定义、边界、为何存在 | 全周 |
| `pattern` | 模式 | ReAct、RAG、Reflection、Multi-agent | W1–W3 |
| `mechanism` | 机制 | Attention、compaction、tool loop | W2–W4 |
| `practice` | 实践 | 工具链、MCP、评测脚本、Harness 清单 | W3 + 第 2 月 |
| `route` | 路线 | 30 天、7 天冲刺、仅 Model 轨 | 元文档 |
| `resource` | 资源 | 官方文档、论文、课程外链索引 | 附录 |
| `glossary` | 术语 | 短条目，链到主概念 | 全库 |

**第二层常用轴**（frontmatter，用于筛选与图谱颜色）：

- `layer`：`prompt` | `context` | `harness` | `model` | `meta`
- `level`：`L0` 科普 · `L1` 会用 · `L2` 能设计 · `L3` 能排障
- `status`：`stub` | `draft` | `stable`
- `lang`：`zh` | `en`（正文可中英并存，但元数据标明主语言）

---

## 4. 单篇文档分层结构（核心模板）

每一篇 **概念页** 固定章节顺序，便于人和 AI 解析（可用标题锚点 `#30s` `#deep` `#examples`）。

```markdown
---
id: agent-workflow-vs-agent
title: Workflow 与 Agent 的区别
type: concept
layer: harness
level: L1
status: draft
prerequisites: []
see_also: [react-pattern, building-effective-agents]
sources:
  - title: Building Effective AI Agents
    url: https://www.anthropic.com/research/building-effective-agents
updated: 2026-05-24
---

# Workflow 与 Agent 的区别

## 30 秒阅读 {#30s}

- **一句话**：Workflow 是固定步骤编排；Agent 由模型根据反馈决定下一步。
- **何时用 Workflow**：步骤稳定、可审计、失败代价低。
- **何时用 Agent**：步骤不确定、需工具反馈、可接受更高成本与评测负担。
- **常见误区**：把 5 个 if-else 叫 Agent。

## 深入讲解 {#deep}

（分小节：定义 → 对比表 → 失败模式 → 与 Context/Harness 的关系）

## 示例 {#examples}

（伪代码 / 对话片段 / 你项目中的反例——可链到 `examples/` 子目录）

## 相关概念

- [[react-pattern]] — Agent 循环的一种实现
- [[context-engineering]] — Agent 多轮时上下文如何组装

## 自检 {#checks}

- [ ] 能举出一个应该用 Workflow 的业务场景
- [ ] 能说出 Agent 多出来的两类工程成本（评测、护栏）

## 学习路线锚点

- 30 天路线：**D1**
- 预计阅读：30s 1min · 深入 15min · 示例 10min
```

### 4.1 中间遇到的其他概念怎么处理？

规则：**正文里第一次出现且可独立成节点的，用 wikilink `[[slug]]`**；若尚无正文，链到 `stubs/slug.md`（自动生成 stub 模板，frontmatter `status: stub`）。

### 4.2 与 Obsidian 图谱的配合

| 机制 | 用途 |
|------|------|
| `[[wikilink]]` | 双向链接，图谱边 |
| `#tag` 与 frontmatter `tags` | 聚类（如 `#rag` `#mcp`） |
| `see_also` / `prerequisites` | Graph Analysis 插件或 Dataview 查询 |
| MOC 笔记 | `maps/agent-stack-moc.md` 手工策展入口 |
| 根目录已有 `.obsidian` | 建议 vault = 整个 `资料整理`，本项目路径 `ai学习/learn-ai/` |

---

## 5. 仓库目录结构（建议）

开源时建议 **独立仓库** `learn-ai`（或 `ai-teaches-ai`），本规划阶段可先建在 `ai学习/learn-ai/` 试点，成熟后再 `git subtree split` 或复制出去。

```text
learn-ai/
├── README.md                 # 项目说明、如何 Obsidian 打开、如何贡献
├── AGENTS.md                 # 给 Cursor/Codex：如何读图谱、写概念页、推路线
├── CONTRIBUTING.md
├── progress.example.yaml     # 学习者进度（fork 后私有）
├── maps/                     # 路线与 MOC（大图谱入口）
│   ├── agent-30d-route.md    # 链到 30天-agent学习路线，但用 [[id]] 引用概念
│   ├── agent-stack-moc.md
│   └── model-fundamentals-moc.md
├── concepts/                 # 原子知识（按 layer 分子目录可选）
│   ├── prompt/
│   ├── context/
│   ├── harness/
│   └── model/
├── patterns/
├── glossary/
├── examples/                 # 较长示例，避免概念页过长
├── stubs/                    # 待撰写占位
├── routes/                   # 可复用路线 YAML + 人类可读 md
│   └── agent-30d.yaml
├── templates/
│   └── concept.md            # 概念页模板
├── scripts/                  # 可选：校验 frontmatter、断链检查
│   └── lint_concepts.py
└── .obsidian/                # 可选：仅子 vault 时需要；或与父 vault 共用
```

**个人学习日志** 仍放 [`30天-agent学习路线/notes/`](./30天-agent学习路线/notes/)，**不**与 `concepts/` 混放。

---

## 6. 学习路线怎么挂到图谱上

### 6.1 三层结构

```text
路线 (route)  →  日程/模块 (module)  →  概念页 (concept)
```

- **路线**：`routes/agent-30d.yaml` 机器可读；`maps/agent-30d-route.md` 人类可读 + wikilink  
- **模块**：对应 W1–W4 或「第 2 月实践」  
- **概念**：每个 D{n} 对应 1–3 个 `concept` id，日历表里只写 id，不写长文  

### 6.2 改造现有 30 天 README 的方式（轻量）

在 [`30天-agent学习路线/README.md`](./30天-agent学习路线/README.md) 的每日表格增加一列 **概念 ID**，例如：

| 天 | 概念 ID |
|----|---------|
| D1 | `[[agent-workflow-vs-agent]]` |

开源库成熟后，日历可自动生成或半自动生成，避免双份维护。

### 6.3 与 `AI知识树大纲` 的关系

| 文档 | 角色 |
|------|------|
| `AI知识树大纲/公司内部-...` | **企业能力树 + 验证方式** → 映射表 `maps/enterprise-bridge.md` |
| `learn-ai/concepts/` | **开源原子知识** |
| 桥接 | 公司节点 ID ↔ 概念 `id` 多对多，不复制全文 |

这样你的开源项目有特点，又不浪费已有调研。

---

## 7. 实施阶段（建议 4 期）

### Phase 0 — 规范落地（1 周，可与 D1–D7 并行）

- [ ] 创建 `learn-ai/` 目录与 `templates/concept.md`
- [ ] 写 `AGENTS.md`（Agent 如何读模板、如何补 stub）
- [ ] 从 D1–D3 抽 **3 篇 stable 概念页** 作为样例
- [ ] 写 `maps/agent-stack-moc.md` + `routes/agent-30d.yaml`（仅 W1 条目）

**验收**：Obsidian 图谱中能看到 ≥10 节点、≥15 条边；无断链（脚本或 Obsidian 插件检查）。

### Phase 1 — 跟学沉淀（W1–W4，约 1 个月）

- 规则：**每学习日 1 概念页**（最低 stub，周末升格 draft）
- W4 结束：`concepts/` 约 25–35 篇，覆盖 30 天主线
- 更新 `30天-agent学习路线` 的概念 ID 列

### Phase 2 — 「AI 教你学」最小闭环（第 2 个月）

- [ ] `progress.yaml` + 推荐下一概念脚本（或 Cursor command）
- [ ] 从 `checks` 自动生成 3 道测验（人工校对）
- [ ] README：fork 后如何用 Agent 学

### Phase 3 — 开源与社区（可选）

- [ ] 独立 GitHub 仓库、LICENSE（建议 MIT）、贡献指南
- [ ] CI：`lint_concepts.py` 检查 frontmatter / 断链
- [ ] 英文 stub 或 bilingual 政策（先中文主干）

---

## 8. 你先做哪几件事（本周可执行）

按优先级：

1. **确认 vault 范围**：整个 `资料整理` 作 Obsidian vault，还是仅 `ai学习/learn-ai` 子 vault。  
2. **创建 `ai学习/learn-ai/`**，复制 `templates/concept.md`，写第一篇：`agent-workflow-vs-agent`（对应 D1）。  
3. **改 30 天表**：D1–D7 增加「概念 ID」列（可先只填已写的）。  
4. **读一遍** [`AI知识树大纲/公司内部-AI学习大纲-分级验证与知识树.md`](../AI知识树大纲/公司内部-AI学习大纲-分级验证与知识树.md)，列出 5 个要对齐的「验证方式」写进模板 `checks` 小节。  
5. **（可选）** 在仓库根 `AGENTS.md` 或 `.cursor/rules` 增加一条：写 AI 学习笔记时优先链到 `learn-ai/concepts/`。

---

## 9. 待你确认的决策

| # | 问题 | 建议默认 |
|---|------|----------|
| 1 | 开源仓库是否独立于 `资料整理`？ | 先子目录试点，稳定后新 repo |
| 2 | 正文语言 | 中文为主，术语保留英文 |
| 3 | 概念 id 命名 | `kebab-case`，与文件名一致 |
| 4 | 30 天路线放哪 | 保留现文件夹；图谱用 `maps/agent-30d-route.md` 引用 |
| 5 | AI 生成内容边界 | 概念页必须列 `sources`；无来源标 `status: draft` |

你确认后，我可以下一步直接生成：`learn-ai/` 骨架、`templates/concept.md`、D1 样例概念页、以及更新 30 天 README 的概念 ID 列。

---

## 10. 进度追踪（规划本身）

- [ ] 已阅读本规划并确认 Phase 0 范围  
- [ ] 已选定 Obsidian vault 范围  
- [ ] 已创建 `learn-ai/` 骨架  
- [ ] 已完成首篇概念样例  
- [ ] 已更新 30 天路线的概念 ID 映射  
