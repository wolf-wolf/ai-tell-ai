## 共用规则（所有 search 子任务）

- **观测日期**：{{DATE}}
- **输出文件（必须写入）**：`trends/{{DATE}}/.research/{{KIND}}.md`
- **禁止**写 `index.md`；只写本任务的 research 笔记
- **必须联网检索**（Trending、官网、arxiv、HN 等），禁止凭记忆编造 Star 数、日期、引语
- 每条候选信号注明：**来源 URL**、**访问日期（{{DATE}}）**、**置信度**（高/中/低）
- 数字无法核实则写「未核实」并标低置信度，不要猜测
- 完成后只汇报：写入路径、候选条数、高置信条数

### 输出 Markdown 结构

```markdown
# Research — {{KIND}} — {{DATE}}

## 检索范围
（用了哪些站点/查询）

## 候选信号
### 1. 标题
- **URL**:
- **metric**:
- **tier 建议**:
- **事实**（可含列表）:
- **置信度**: 高|中|低
- **来源**: URL + 日期

（≥3 条候选，宁缺毋滥）
```
