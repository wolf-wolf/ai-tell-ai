# ai-tell-ai 概念文章编写规则

每篇文章按以下顺序组织，不可颠倒。前两节有格式与内容逻辑要求，其余节由 agent 自行决策。

---

## 文章标准结构

1. **Frontmatter**
2. **# 标题**
3. **> [!tip] 核心本质**
4. **## 生命周期与演进**
5. **## 核心原理**（标题可按主题自定）
6. **## 实践与应用**（标题可按主题自定）
7. **## 进一步阅读**

---

## Frontmatter

```yaml
---
tags: []
aliases: []
related: []
stability: short | mid | long
layer: model | application | …
updated: YYYY-MM-DD
---
```

可选字段：`prerequisites`、`title`、`difficulty`。

---

## 核心本质

**格式**：Obsidian Callout，类型 `tip`：

```markdown
> [!tip] 核心本质
> …
```

**内容逻辑**：用一句话点透这个技术/概念是什么，并通过反事实推理（如果没有它，系统会在哪个环节出问题）说明它存在的必然性。

---

## 生命周期与演进

**格式**：四个黑体标签，加冒号起头，标签间空一行，不使用列表符：

```markdown
**当前定位**：…

**预期寿命**：…

**近期演进**：…

**终极威胁**：…
```

**内容逻辑**：给读者一个上帝视角——这个技术当前处于什么阶段、大概还能存在多久、正在往哪个方向演变、最终会被什么取代或吸收。

---

## 其余各节

内容由 agent 根据主题自行决策，不作约束。

有一条拆分原则：若某技术衍生出独立的子主题（如工程实践方法论、选型指南、对比分析等），须另起独立文章，在「进一步阅读」中链接，不与原理混在同一篇。

### 正文不写库内文章边界

单篇只讲**本概念自身的机制、约束与事实**。不要在正文用 callout、段落或表格专门说明「与 [[某篇兄弟文]] 的边界」「对照」「差在哪」——那是库内导航 meta，不是读者理解本篇所必需。

**允许**：

- `## 进一步阅读` 用 wikilink 指向相关节点，**一行**说明读者能读到什么（机制延伸、上下游、通用抽象），不写「边界对照」。
- frontmatter `related` / `prerequisites` 列关联。
- 正文为论证**本篇机制**做必要对比：本篇内子概念分工（如持久记忆 vs 会话搜索）、外部产品/论文/标准差异。

**禁止**：

- `> **与 [[knowledge-extraction]] / [[knowledge-fusion]] 的边界**：…`
- `这与 [[某文]] 里的 X 也不同：…`（若目的仅为划清兄弟文边界）
- 延伸阅读条目写成「与本文的边界对照」

易混淆处应写**机制差异本身**（做了什么、没做什么），不要写成「我们这篇 vs 那篇」。

### 正文标记约定

与 Obsidian snippet（`ai-bold` / `ai-inline-code`）配合，避免同一段落里高亮过密。

#### 1. 适度使用加粗

加粗是**稀疏强调**，不是正文默认样式。遵守：

| 标记 | 用途 | 用量 |
| --- | --- | --- |
| `**加粗**` | 生命周期标签、表格行首类别名 | **极稀疏**：正文段落原则上不加粗；生命周期四标签（`**当前定位**：` 等）与对照表行首可保留 |
| `` `反引号` `` | 工具名、文件名、配置键、API | 按需；Obsidian 中以 muted 等宽底框展示 |

- 同一概念全文只强调一次；再次提及用普通文字或 wikilink，不重复加粗。
- Callout「核心本质」内原则上不加粗——概念由叙述与链接承担。
- 一段内同时出现多个加粗或「加粗 + 行内代码」时，优先去掉加粗，只保留必要的 `` `反引号` ``。

#### 2. 文内导引须带锚点链接

正文中的章节导引（如 `§2.2`、`见 §2.3`、`（§5）`、`对应 §工具怎么给`）**禁止裸写**，必须写成可点击的 Obsidian 锚点链接：

```markdown
<!-- 本篇内 -->
详见 [[#2.2 冻结快照（Frozen Snapshot）与双态一致|§2.2]]。

<!-- 无编号小节（本篇） -->
对应 [[#工具怎么给|§工具怎么给]]。

<!-- 它篇（少用；优先 wikilink 到概念节点） -->
见 [[hermes-agent-memory#2.2 冻结快照（Frozen Snapshot）与双态一致|§2.2]]。
```

- `#` 后为**目标小节标题全文**（与文内 `##` / `###` / `####` 标题一致），`|` 后为显示文字（通常保留 `§` 与编号或短标题）。
- 改小节编号、标题或层级时，须同步更新所有指向该小节的锚点链接（见下文「重命名或结构调整时同步引用」）。
- Mermaid 节点、图注里的 `§` 导引，若读者可能需要跳转，同样应带锚点；纯装饰性标注可例外。

### Obsidian 样式 snippet

- 行内代码：`bash tools/obsidian-inline-code-snippet/install.sh`（muted 等宽底框）
- 正文加粗：`bash tools/obsidian-bold-snippet/install.sh`（纯正文字重，无底色）
- 表格字号：`bash tools/obsidian-table-snippet/install.sh`（比正文小一号）
- 正文排版：`bash tools/obsidian-reading-snippet/install.sh`（字间距 + 行高）

---

## 增补与修改规则

适用于对**已有文章**新增内容、扩写段落、或优化措辞时。

### 增补前先判断归属

新内容应先问「属于哪篇」：

- 与当前文章核心概念直接相关 → 补在本篇适当位置。
- 已有兄弟文档覆盖（或更适合放在那里）→ 不重复写，用 wikilink 交叉引用并一句话说明区别。
- 是独立的子主题（工程实践、选型指南、对比分析等）→ 另起独立文章，不写进原理篇。

**冗余是最常见的问题**：如果同一事实在本篇两处都出现，或与已链接文档内容重叠，必须合并或删除一方，只保留一处权威位置。

### 扩写要有因果密度，不是篇幅堆砌

「内容太少、太浅」的正确解法是补**机制**与**因果链**，而不是增加 bullet 数量：

- 补「为什么」：这个现象/规则背后的原因是什么？
- 补「如果不这样会怎样」：反事实推理，说明约束存在的必然性。
- 补「与 X 的区别」：在易混淆处写清机制差异（本篇内概念或外部产品/标准）；**不要**在正文划「与 [[兄弟文]] 的边界」（见上文「正文不写库内文章边界」）。
- 补「产品/场景差异」：若「因产品而异」，必须逐项说清差异是什么，不能一笔带过。

禁止只靠增加表格行数或 bullet 数量来「扩写」——若没有新的观点或证据，宁可不改。

### 核心本质只写概念本身

`> [!tip] 核心本质` 的内容必须是**这个技术/概念是什么**，以及**没有它系统会在哪里出问题**。

禁止写：
- 「本篇讲…」「本文覆盖…」「本节分为三部分」——这是目录，不是本质。
- 对文章结构的描述，哪怕包裹在概念语言里（如「本篇分别讲清运行时加载与库演化两条轴线」）。

### 正文 `##` 节首：机制优先，不写目录复述

`##` 标题已在目录中说明范围。节首**直接写**机制、问题、结论或与上节的衔接——不要再用「本节讲…」「本节说明…」「本节回答…」「本节分为…」把子主题复述一遍。

- **允许**：一句不重复标题的切入（矛盾、设计抉择、反事实、必要背景）。
- **禁止**：把即将出现的 `###` 标题列成清单当作「路标」。

对比：

- ❌ `本节讲跨会话短事实：存在哪、怎么读进 context、怎么写。`
- ✅ `通用 Agent 记忆常走两条极端：完全不持久化，或把全量历史当记忆。`

`###` 子节同理：标题已能扫读时，首句直入内容，勿写「本小节介绍…」。

### 手术式修改，不扩大改动范围

修改某段时，只动需要改的内容：

- 不要顺手「改进」相邻的措辞、格式或结构。
- 不要在修改 A 段的同时重排 B 段的列表。
- 若发现无关的问题，可以提出，但除非被明确要求，否则不动。

### 重命名或结构调整时同步引用

若修改了标题、文件名、章节锚点：

- 批量更新全库所有 wikilink（含锚点 `[[file#section]]`）。
- 更新 `map.md` 的索引条目与描述。
- 不保留纯为兼容旧名的 aliases，除非有具体的 Obsidian 搜索需求。

---

## Agent 写作 skill

在 Cursor 中写/改 `docs/` 知识节点时，可显式调用项目 skill **ai-tell-ai-knowledge-doc**（`.cursor/skills/ai-tell-ai-knowledge-doc/`）。它将本文的结构约束与 `tech-topic-overview-doc` 的成文能力（因果密度、编号小节、Mermaid、延伸阅读溯源）合并；细则见该目录下的 `reference.md` 与 `templates/knowledge-article.md`。

评审与打分见根目录 `article-scoring.md`（G/Q + `done` 契约 + §2.5 执行契约 + §5.4 外链验真）。迭代打磨用 Cursor skill **article-scoring-evolve**（`.cursor/skills/article-scoring-evolve/`）：默认全流程（合规→突破），四席 Task 分离、每轮落盘 `_meta/article-reviews/`；子 agent 限流/失败时 `run_status=incomplete`，**不得**假装成功。

