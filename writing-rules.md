# ai-tell-ai 概念文章编写规则

每篇文章按以下顺序组织，不可颠倒。前两节有格式与内容逻辑要求，其余节由 agent 自行决策。

---

## 文章标准结构

1. **Frontmatter**
2. **# 标题**
3. **> [!tip] 核心本质**
4. **## 生命周期与演进**
5. **## 核心原理**（标题可按主题自定）
6. **正文 `##` 节**（按「概念文推荐脊柱」与阅读约束组织）
7. **## 要点收束**（可选，在进一步阅读之前）
8. **## 进一步阅读**

---

## Frontmatter

```yaml
---
tags: []
aliases: []
related: []
stability: short | mid | long
layer: model | application | …
updated: YYYY-MM-DD
---
```

可选字段：`prerequisites`、`title`、`difficulty`。

---

## 核心本质

**格式**：Obsidian Callout，类型 `tip`：

```markdown
> [!tip] 核心本质
> …
```

**内容逻辑**：用一句话点透这个技术/概念是什么，并通过反事实推理（如果没有它，系统会在哪个环节出问题）说明它存在的必然性。

---

## 生命周期与演进

**格式**：四个黑体标签，加冒号起头，标签间空一行，不使用列表符：

```markdown
**当前定位**：…

**预期寿命**：…

**近期演进**：…

**终极威胁**：…
```

**内容逻辑**：给读者一个上帝视角——这个技术当前处于什么阶段、大概还能存在多久、正在往哪个方向演变、最终会被什么取代或吸收。

---

## 文章类型与内容契约

库内文章**不是同一写法**。动笔前先定**主类型**（一篇只选一个主轴；混合型须拆文或在大纲写明主次），再按对应契约组织正文 `##`。Frontmatter、核心本质、生命周期、进一步阅读的**节序不变**；变的是正文要**必答哪些问题**、证据从哪里来、机制节要不要源码锚点。

### 类型总览

| 主类型 | 典型目录 | 读者读完应能 | 篇幅软区间 |
| --- | --- | --- | --- |
| **技术与机制** | `model/mechanism/`、`algorithms/`、`model/training/`、`agent/retrieval/` 中偏流水线机制 | 说清**干什么、缺了哪环断、基本实现路径**；必要时指到核心实现 | 6k–10k |
| **概念与范式** | `model/concept/`、`agent/core/`、`agent/pattern/`、`methodology/` | 建立**整体心智模型**，说清**子概念分工**与定义逻辑 | 4k–8k |
| **框架与项目** | `docs/latest/`、闭源产品观测文 | 说清**实现原理、怎么用、优劣势与适用场景**（有证据） | 10k–15k |
| **方法论与治理** | `methodology/`、`agent/skill/`、`agent/context/` 中偏落地 | 给出**可执行框架 + 检查单 + 反模式** | 6k–10k |
| **数据工程** | `data/` | 说清**数据从哪来、怎么洗/标/验、质量门禁** | 6k–10k |
| **速览与地图** | `topic-overviews/`、根 `map.md` 导览 | **导航**：主题边界、读哪篇深潜；不替代机制正文 | 3k–6k |

目录归属见 [docs/STRUCTURE.md](docs/STRUCTURE.md) 决策树。类型与目录**多数对应、非一一绑定**：例如 `agent/retrieval/rag.md` 偏概念与范式，`algorithms/bm25.md` 偏技术与机制；拿不准时以「读者主要来问什么」为准。

### A. 技术与机制

回答「**这个机制/算法在系统里解决什么问题、怎么做到**」——不是产品说明书，也不是纯名词解释。

**必答四问**（正文须能追溯回答，不可只堆定义）：

| 问题 | 写法要求 |
| --- | --- |
| **干什么** | 在更大流水线/系统中的**位置与职责**；一句可复述的输入→输出 |
| **有 vs 没有** | 反事实：去掉它，**哪一环断**（延迟、质量、成本、正确性）；宜具象场景，不只抽象形容词 |
| **基本实现** | 阅读梯：**场景→直觉→形式化（公式/步骤/复杂度/关键超参）→设计含义**；表图服务机制，不为凑表 |
| **核心实现** | 当行为可核对时：给**关键步骤、伪代码或短节选**；开源实现优先 **`path#L` 深链**（见 [reference.md — 源码写法](.cursor/skills/ai-tell-ai-knowledge-doc/reference.md#源码分析与实现深潜写法)），**禁止**大段代码替代理清 |

**推荐正文脊柱**：

```text
问题语境（无它哪环断）
  → 边界与对比（与易混算法/机制；对照表宜靠前）
  → 核心机制（直觉→形式化→复杂度/超参）
  → 实现要点（可选：伪代码、关键源码锚点、工程权衡）
  → 实践与落地（参数选型、何时不用、反模式）
  → 要点收束（可选）→ 进一步阅读
```

**证据**：论文/官方文档/可复现实现；定量断言须可溯源。`stability: permanent|long` 为主。

### B. 概念与范式

回答「**我们为什么这样划分、整体怎么理解、局部各管什么**」——建立可迁移的心智模型，而非绑定某一产品 API。

**必答三问**：

| 问题 | 写法要求 |
| --- | --- |
| **定义逻辑** | **第一性原理**：为何需要这个概念、与上下游概念的**因果**；禁止只给词典定义 |
| **整体概念** | 一张「系统图景」：边界、主要对象、典型数据/控制流；可用 Mermaid，≤ ~20 节点 |
| **局部概念** | 子概念/变体/层次：**各管什么、不包什么**；用本篇内分工表或机制叙述，**禁止**正文写「与 [[兄弟文]] 对齐/分歧」（见下文「正文不以兄弟文为标尺」） |

**推荐正文脊柱**（即默认「概念文推荐脊柱」）：

```text
问题语境（无它哪环断、具象场景）
  → 边界与对比（与易混范式/机制）
  → 整体模型（对象、关系、典型流）
  → 局部概念分工（子类型、层次、常见误读）
  → 实践含义（何时选用、设计后果；深选型另文）
  → 要点收束（可选）→ 进一步阅读
```

**篇幅与拆分**：一个心智模型 + 边界 + 何时用/不用；工程实践、长选型、多产品横评**另起独立文**。

### C. 框架与项目

回答「**这个开源/闭源项目核心怎么实现、怎么用、值不值得用**」——可点名版本，默认有时效。

**必答四问**：

| 问题 | 写法要求 |
| --- | --- |
| **核心实现原理** | **机制优先**：主循环、状态边界、扩展点、与库内抽象概念的对应（写机制，不写 README 目录复述） |
| **怎么用** | **最小可用路径**：安装/配置门槛、关键 API 或交互面、典型任务端到端；观测日期与版本量级 |
| **优劣势** | **场景化对比**：适合什么、不适合什么；须有证据（官方文档、Issue、benchmark、源码行为），禁止空泛「灵活/强大」 |
| **风险与边界** | 许可、厂商锁定、已知限制、docs vs code 分歧 |

**执行顺序**：产品/源码文须先走 [task-doc-codebase-mechanism-deep-dive](.cursor/skills/task-doc-codebase-mechanism-deep-dive/SKILL.md)（source map、≥3 文件深读），再按 [reference.md — 源码写法](.cursor/skills/ai-tell-ai-knowledge-doc/reference.md#源码分析与实现深潜写法) 落盘。

**推荐正文脊柱**：

```text
问题语境（为何需要这个框架/产品）
  → 架构与核心机制（数据流、主循环、扩展点）
  → 实现要点 / Prompt 契约（可选；表 + 节选 + 逻辑怎么读）
  → 使用方式（快速上手 + 常见集成模式）
  → 选型：优势、劣势、与替代方案的场景对比
  → 要点收束（可选）→ 进一步阅读（含 ### 源码与 Prompt 原文）
```

**证据**：**强制联网** + 仓库/官方文档；`stability: short|mid`，正文关键事实旁标注**观测日期**。

### D. 方法论与治理

回答「**在真实工程里怎么按这套范式做、怎么验收**」。偏 `methodology/`、`agent/skill/`、`agent/context/` 等。

**必答**：问题语境 → **操作框架**（步骤/角色/产物）→ **落地检查单**（≥3 条可执行）→ **反模式**（常见失败与修复）→ 延伸阅读可溯源（官方 > 仓库 > 高质量二手）。

可与 **B. 概念与范式** 组合：概念文讲「是什么、为何如此划分」；方法论文讲「怎么做、怎么验」——**应拆成两篇**，通过 `## 进一步阅读` 互链。

### E. 数据工程

回答「**这批数据怎么做成可用数据集**」（与 `model/training/` 的「训练为何需要某种数据」区分）。偏 `data/`。

**必答**：数据来源与适用性 → 清洗/去重/标注规范 → **质量指标与门禁** → 版本/血缘/回滚 → 与下游训练或 RAG 入库的接口。

### F. 速览与地图

回答「**这一主题下有哪些节点、先读哪篇**」。偏 `topic-overviews/`、索引页。

**必答**：主题边界（包什么、不包什么）→ 分域导航表或图 → 每链一行「在那篇能读到什么」→ **不**在速览里写深机制（链到 wiki 节点）。

### 写前：类型选择与提纲

新建或结构性改写时，在 `_meta/outline/<slug>.md` **Phase A** 增加一行：

```markdown
**主类型**：技术与机制 | 概念与范式 | 框架与项目 | …（若混合，写主次与是否应拆文）
```

Phase B 论证提纲的 `##` 标题须与对应类型的**推荐脊柱**对齐；自检：**标题测试**时只读标题应能看出该类型必答问题是否覆盖。

### 跨类型通则

- **核心本质**、**生命周期**、**人类阅读约束**、**专有名词**、**正文不以兄弟文为标尺**：各类型均适用。
- **技术与机制**、**框架与项目**：涉及实现断言时，须有源码/官方出处；框架文走源码深潜流程。
- **概念与范式**、**速览**：通常不逐文件溯源；链到 `latest/` 或 `algorithms/` 兄弟文即可。
- **选型长文、排障大全、多产品横评**：默认**独立成文**，不在原理/概念篇内展开。

---

## 其余各节

正文 `##` 标题按主题自定，但须遵守下文 **人类阅读约束** 与 **概念文推荐脊柱**。有一条拆分原则：若某技术衍生出独立的子主题（如工程实践方法论、选型指南、对比分析等），须另起独立文章，在「进一步阅读」中链接，不与原理混在同一篇。

### 人类阅读约束（写前 / 改前必读）

读者同时在做四件事：**解码术语**、**拼机制图景**、**挂接旧知识**、**预测下一句是否回答刚产生的疑问**。工作记忆一次只能稳定承载约 **3–4 个新块**；同屏术语过多、内容跳跃、先 formal 后直觉，都会让人「云里雾里」——与篇幅长短无关。

| 约束 | 规则 |
| --- | --- |
| **阅读梯** | 每个关键 `###` 内部顺序：**具象场景或失败模式 → 一句话直觉 → 形式定义/机制/表 → 设计含义或适用条件**；禁止段首堆论文名、产品名或未定义术语 |
| **术语预算** | 每个 `###` **新引入术语 ≤ 3 个**（中文（English）算 1 个）；超出拆到下一 `###` 或 `## 进一步阅读` |
| **段落** | 一段一主题；**段首即中心句**；中文正文 **≤ 4 行**为佳、**不超过 7 行**；优先短句、并列句 |
| **滑梯** | 段末与下段首用**因果或疑问**衔接；禁止「下文将介绍…」式目录预告 |
| **表图** | 表/图**前**写为何需要；**后**写读者应带走的结论；禁止 bullet 中间孤悬大图 |
| **读者契约** | `核心本质` 后 **3–4 句**：适合谁、读完能决策/复述什么、读到哪可停（非目录） |
| **要点收束** | `## 进一步阅读` **前**可选 `## 要点收束`：3–5 条可复述结论，不重复链接列表 |

**核心本质**内原则上不堆 wikilink、不并列多个系统/论文名——留给正文按阅读梯展开。

### 概念文推荐脊柱（类型 B 默认）

正文 `##` 宜按读者认知顺序组织（标题可改，顺序不宜乱）。**完整类型契约**见上文 [文章类型与内容契约](#文章类型与内容契约)；概念与范式类默认采用下列脊柱：

```text
问题语境（无它哪环断、具象场景）
  → 边界与对比（与易混机制表，宜靠前）
  → 核心机制（问题→原因→设计含义）
  → 实践与落地（选型、参数、反模式）
  → 要点收束（可选）
  → 进一步阅读
```

`## 生命周期与演进` 讲**时间维**；不替代「问题语境」。机制节内：**先例子/隐喻，再公式与参数表**。

### 篇幅软指引（非 KPI）

篇幅**不作为** `article-scoring.md` 的评分维或 `done` 停止条件；下列区间仅供写前 / 改前**估算 scope**，避免为凑字数扩写，或在应拆时不拆。

**计数方式**：正文 Markdown 字符数（含中文标点；frontmatter 与代码块计不计入均可，同一篇内前后一致即可）。粗估可用 `wc -m <path>`。**类型与区间对应**见 [文章类型与内容契约 — 类型总览](#类型总览)。

| 类型 | 软目标区间 | 说明 |
| --- | --- | --- |
| 概念与范式（`agent/core`、`model/concept` 等） | **4k–8k** | 一个心智模型 + 边界 + 何时用 / 不用；短于 4k 若 Q 维已过亦可 |
| 技术与机制（`algorithms`、`model/mechanism` 等） | **6k–10k** | 直觉 → 形式化 → 含义；表图服务机制 |
| 框架与项目（`latest/`、Prompt 契约文） | **10k–15k** | 证据链长；完整 Prompt、排障清单优先放 `## 进一步阅读` |
| 速览与地图（`topic-overviews/`） | **3k–6k** | 地图式导览；深机制链向兄弟文 |

| 信号 | 动作 |
| --- | --- |
| 草稿 **>10k** | 先问是否混入应独立成文的子主题（工程实践、选型、深对比、长排障）；能拆则拆 |
| 草稿 **>12k** | **默认应拆**；若保留单篇，须在 `_meta/outline/<slug>.md` 写 **「不拆分理由」**（论证线是否仍为一、拆分是否会伤读者路径） |
| 为达标加 bullet / 表 / Mermaid 却不补机制 | 违规，见下文「扩写要有因果密度」与 `article-scoring.md` §五.2 |

**停笔优先于凑字数**：以 G/Q + `done`、读者契约「读到哪可停」、以及上文**拆分原则**为准；**不得以「每篇万字」为写作目标**。

### 先提纲，再写；改写也先提纲

新建或**结构性改写** `docs/` 文章时，**须先落盘提纲**再写正文：

1. **Phase A — 阅读契约**：读者是谁、读完能复述什么、前置 `prerequisites` 是否够；**主类型**（见 [文章类型与内容契约](#文章类型与内容契约)）
2. **Phase B — 论证提纲**：只写 `##` / `###` 标题 + 每节一句「本节回答 ___」；扫标题应看出问题→机制→应用  
3. **Phase C — 术语表**（可选）：各 `###` 术语预算与首次出现顺序  
4. **Phase D — 起草**：按阅读梯写 prose  
5. **Phase E — 验收**：**标题测试**（只读标题能否说出论证线）、**段首测试**（只读各 `###` 首段能否拼出主线）

提纲路径：`_meta/outline/<slug>.md`（`<slug>` 与文章路径一致，如 `docs/algorithms/ann.md` → `_meta/outline/algorithms/ann.md`）。**无提纲不得全文重写**；手术式小改可跳过。

对比：

```markdown
<!-- ❌ 段首 -->
SkillRL 的 SkillBank 与 COS-PLAY 的效应契约在检索后过滤…

<!-- ✅ 段首（阅读梯） -->
当库里有千万条向量时，对用户问题算一遍全库相似度，单次查询就要千万次——延迟与成本都不可接受。近似最近邻（ANN）的做法是：预先组织数据，查询只摸一小部分候选。
```

### 正文不以兄弟文为标尺

单篇只讲**本概念自身的机制、约束与事实**。库内已有文章**不是**范本或对照标准——它们也会过时、有瑕疵；新文**不得**围着旧文组织结构，或向读者证明「与某篇一致 / 对齐 / 同构」。

下列写法属于**库内导航 meta**，读者理解本篇机制并不必需，**禁止**出现在正文（含 callout、表格专段、小节标题）：

| 类型 | 禁止示例 |
| --- | --- |
| 边界对照 | `与 [[knowledge-extraction]] / [[knowledge-fusion]] 的边界：…` |
| **对齐 / 映射** | `与 [[skill-loading-library]] 的 Discovery→Activation **对齐处在于**…`、`**分歧处在于**…`、`同构于 [[skill-governance]]` |
| 借旧文当脚手架 | 用另一篇的小节名、阶段名、四动作框架当本篇叙述主轴，只为说明「讲的是同一回事」 |
| 延伸阅读 meta | 「与本文的边界对照」「差在哪」 |

**允许**：

- `## 进一步阅读`：wikilink + **一行**说明读者在那篇能读到什么（机制延伸、上下游），**不写**对齐 / 边界 / 分歧。
- frontmatter `related` / `prerequisites` 列关联。
- 正文论证**本篇机制**：因果、约束、反事实；本篇内子概念分工（如持久记忆 vs 会话搜索）；与**外部**产品 / 论文 / 标准的差异（有证据时）。
- **概念相关**时一句带过即可，如「同属技能载体，见 [[skill]]」「运行时 listing 见 [[skill-loading-library]]」——只陈述**话题相关**，不展开对齐表。

**写法对照**（机制写在本篇，链接放延伸阅读）：

```markdown
<!-- ❌ -->
与 [[skill-loading-library]] 的 Discovery→Activation 对齐处在于：不要全库灌入；分歧处在于检索器实现。

<!-- ✅ -->
决策层只注入与当前任务相关的技能子集，不要全库灌入（渐进式披露）。宿主侧 listing 预算与路由见 [[skill-loading-library]]。
```

易混淆处写**机制本身**（做了什么、没做什么、在什么条件下成立），不要写成「本篇 vs 那篇」或「对齐 / 分歧表」。

### 正文标记约定

与 Obsidian snippet（`ai-bold` / `ai-inline-code`）配合，避免同一段落里高亮过密。

#### 1. 适度使用加粗

加粗是**稀疏强调**，不是正文默认样式。遵守：

| 标记 | 用途 | 用量 |
| --- | --- | --- |
| `**加粗**` | 生命周期标签、表格行首类别名 | **极稀疏**：正文段落原则上不加粗；生命周期四标签（`**当前定位**：` 等）与对照表行首可保留 |
| `` `反引号` `` | 工具名、文件名、配置键、API | 按需；Obsidian 中以 muted 等宽底框展示 |

- 同一概念全文只强调一次；再次提及用普通文字或 wikilink，不重复加粗。
- Callout「核心本质」内原则上不加粗——概念由叙述与链接承担。
- 一段内同时出现多个加粗或「加粗 + 行内代码」时，优先去掉加粗，只保留必要的 `` `反引号` ``。

#### 2. 专有名词：中文（英文）

正文叙述（非代码）**禁止**中英夹杂——不要把英文专有名词、技术术语、缩写直接嵌进中文句子里。

**适用**：技术概念、范式名、论文/方法名、行业缩写，以及读者需要对照英文原文的术语（如 RAG、Agent、embedding、tokenizer）。

**写法**：**中文全称或惯用译名** + **括号内英文**。英文只出现在括号里，不单独散落在句中。

```markdown
<!-- ✅ -->
检索增强生成（RAG）把查询先映射到文档术语，再召回片段。
智能体（Agent）在工具循环里调用检索。
嵌入（Embedding）向量用于相似度排序。

<!-- ❌ -->
用 RAG 增强检索；Agent 在 loop 里调工具；用 embedding 做排序。
混合写「RAG 检索增强」或句中随意夹 English 词。
```

| 场景 | 写法 |
| --- | --- |
| 专有名词 / 技术术语 / 缩写 | 中文（English），如 思维链（Chain-of-Thought）、大语言模型（LLM） |
| 首次出现后 | 后文可只用中文，不必每处重复括号；若后文需对照英文文献再写一次即可 |
| 代码、命令、配置键、文件名、API 标识符 | 仍用 `` `反引号` ``，规则不变 |
| 无稳定中文译名且业界普遍用英文的产品/项目名 | 可保留英文，但**首次**须 `中文说明（ProductName）` 或脚注交代；勿一句里中英混排 |
| 标题、`核心本质`、表格「行首类别名」 | 同上：优先「中文（English）」，避免标题裸写英文缩写 |

**自检**：通读一段，若去掉 `` `代码` `` 后仍出现裸英文单词（非括号内），即违规，应改为「中文（English）」或收入反引号（仅当确为标识符时）。

#### 3. 文内导引须带锚点链接

正文中的章节导引（如 `§2.2`、`见 §2.3`、`（§5）`、`对应 §工具怎么给`）**禁止裸写**，必须写成可点击的 Obsidian 锚点链接：

```markdown
<!-- 本篇内 -->
详见 [[#2.2 冻结快照（Frozen Snapshot）与双态一致|§2.2]]。

<!-- 无编号小节（本篇） -->
对应 [[#工具怎么给|§工具怎么给]]。

<!-- 它篇（少用；优先 wikilink 到概念节点） -->
见 [[hermes-agent-memory#2.2 冻结快照（Frozen Snapshot）与双态一致|§2.2]]。
```

- `#` 后为**目标小节标题全文**（与文内 `##` / `###` / `####` 标题一致），`|` 后为显示文字（通常保留 `§` 与编号或短标题）。
- 改小节编号、标题或层级时，须同步更新所有指向该小节的锚点链接（见下文「重命名或结构调整时同步引用」）。
- Mermaid 节点、图注里的 `§` 导引，若读者可能需要跳转，同样应带锚点；纯装饰性标注可例外。

### Obsidian 样式 snippet

- 行内代码：`bash tools/obsidian-inline-code-snippet/install.sh`（muted 等宽底框）
- 正文加粗：`bash tools/obsidian-bold-snippet/install.sh`（纯正文字重，无底色）
- 表格样式：`bash tools/obsidian-table-snippet/install.sh`（100% 页宽 + 比正文小一号）
- 正文排版：`bash tools/obsidian-reading-snippet/install.sh`（字间距 + 行高）
- Mermaid 增强：`bash tools/obsidian-mermaid-preview/install.sh`（流程/架构图）
- ECharts 散点/坐标图：`bash tools/obsidian-echarts/install.sh`（` ```echarts ` JSON 块；**非**社区插件市场，与本仓库 `ai-*` 插件同装）

**图示分工**：流程、数据流、架构 → ` ```mermaid `；散点、折线、带坐标轴数值图 → ` ```echarts `（Obsidian 内渲染；Git/Cursor 仍见源码，宜保留坐标表作 fallback）。

---

## 增补与修改规则

适用于对**已有文章**新增内容、扩写段落、或优化措辞时。

### 增补前先判断归属

新内容应先问「属于哪篇」：

- 与当前文章核心概念直接相关 → 补在本篇适当位置。
- 已有兄弟文档覆盖（或更适合放在那里）→ 不重复写，在 `## 进一步阅读` 用 wikilink 指向即可；正文不划边界、不对齐。
- 是独立的子主题（工程实践、选型指南、对比分析等）→ 另起独立文章，不写进原理篇。

**冗余是最常见的问题**：如果同一事实在本篇两处都出现，或与已链接文档内容重叠，必须合并或删除一方，只保留一处权威位置。

### 扩写要有因果密度，不是篇幅堆砌

「内容太少、太浅」的正确解法是补**机制**与**因果链**，而不是增加 bullet 数量：

- 补「为什么」：这个现象/规则背后的原因是什么？
- 补「如果不这样会怎样」：反事实推理，说明约束存在的必然性。
- 补「与 X 的区别」：在易混淆处写清机制差异（本篇内概念或外部产品/标准）；**不要**在正文划「与 [[兄弟文]] 的边界 / 对齐 / 分歧」（见上文「正文不以兄弟文为标尺」）。
- 补「产品/场景差异」：若「因产品而异」，必须逐项说清差异是什么，不能一笔带过。

禁止只靠增加表格行数或 bullet 数量来「扩写」——若没有新的观点或证据，宁可不改。

### 核心本质只写概念本身

`> [!tip] 核心本质` 的内容必须是**这个技术/概念是什么**，以及**没有它系统会在哪里出问题**。

禁止写：
- 「本篇讲…」「本文覆盖…」「本节分为三部分」——这是目录，不是本质。
- 对文章结构的描述，哪怕包裹在概念语言里（如「本篇分别讲清运行时加载与库演化两条轴线」）。

### 正文 `##` 节首：机制优先，不写目录复述

`##` 标题已在目录中说明范围。节首**直接写**机制、问题、结论或与上节的衔接——不要再用「本节讲…」「本节说明…」「本节回答…」「本节分为…」把子主题复述一遍。

- **允许**：一句不重复标题的切入（矛盾、设计抉择、反事实、必要背景）。
- **禁止**：把即将出现的 `###` 标题列成清单当作「路标」。

对比：

- ❌ `本节讲跨会话短事实：存在哪、怎么读进 context、怎么写。`
- ✅ `通用 Agent 记忆常走两条极端：完全不持久化，或把全量历史当记忆。`

`###` 子节同理：标题已能扫读时，首句直入内容，勿写「本小节介绍…」。

### 手术式修改，不扩大改动范围

修改某段时，只动需要改的内容：

- 不要顺手「改进」相邻的措辞、格式或结构。
- 不要在修改 A 段的同时重排 B 段的列表。
- 若发现无关的问题，可以提出，但除非被明确要求，否则不动。

### 重命名或结构调整时同步引用

若修改了标题、文件名、章节锚点：

- 批量更新全库所有 wikilink（含锚点 `[[file#section]]`）。
- 更新 `map.md` 的索引条目与描述。
- 不保留纯为兼容旧名的 aliases，除非有具体的 Obsidian 搜索需求。

---

## Agent 写作 skill

在 Cursor 中写/改 `docs/` 知识节点时，可显式调用项目 skill **ai-tell-ai-knowledge-doc**（`.cursor/skills/ai-tell-ai-knowledge-doc/`）。它将本文的结构约束与 `tech-topic-overview-doc` 的成文能力（因果密度、编号小节、Mermaid、延伸阅读溯源）合并；细则见该目录下的 `reference.md` 与 `templates/knowledge-article.md`。

评审与打分见根目录 `article-scoring.md`（G/Q + `done` 契约 + §2.5 执行契约 + §5.4 外链验真）。迭代打磨用 Cursor skill **article-scoring-evolve**（`.cursor/skills/article-scoring-evolve/`）：默认全流程（合规→突破），四席 Task 分离、每轮落盘 `_meta/article-reviews/`；子 agent 限流/失败时 `run_status=incomplete`，**不得**假装成功。

